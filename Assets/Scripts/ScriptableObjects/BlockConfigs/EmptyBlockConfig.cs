using UnityEngine;

namespace ScriptableObjects.BlockConfigs
{
    [CreateAssetMenu(fileName = "New EmptyBlockConfig", menuName = "Create Empty Block Config")]
    public class EmptyBlockConfig : BaseBlockConfig
    {
    }
}